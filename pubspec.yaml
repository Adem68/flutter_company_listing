name: flutter_company_listing
description: Flutter Company Listing Project

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  cached_network_image: ^3.0.0
  carousel_slider: ^4.0.0-nullsafety.0
  freezed_annotation: ^0.14.1
  http: ^0.13.1
  json_serializable: ^4.1.0
  mdi: ^5.0.0-nullsafety.0
  octo_image: ^1.0.0+1
  provider: ^5.0.0
  url_launcher: ^6.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutrlint: ^2.0.0
  build_runner: ^1.12.2
  freezed: ^0.14.1+2

flutter:
  uses-material-design: true

  assets:
    - assets/
